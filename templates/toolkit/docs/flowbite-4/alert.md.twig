{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name) }}
{% endblock %}

{% block usage %}
{{ toolkit_code_usage(kit_id.value, component.name) }}
{% endblock %}

{% block examples %}
### Variant
{{ toolkit_code_example(kit_id.value, component.name, 'Variant', { height: '600px' }) }}

### Action

Use `Alert:Action` to add a button or other action element to the alert.

{{ toolkit_code_example(kit_id.value, component.name, 'Action') }}

### Borders

{{ toolkit_code_example(kit_id.value, component.name, 'Border') }}
{% endblock %}
