<section class="grid min-h-full min-w-0 gap-8 bg-black p-8 md:h-full md:min-h-0 md:grid-cols-3 md:gap-6 md:p-6 lg:gap-8 lg:p-8 xl:gap-16 xl:p-16" {{ attributes }}>
    {% for key, example in this.examples %}
        <twig:RemovalExample key="removal-example-{{ key }}" :example="example" />
    {% endfor %}
</section>
