{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name, {height: '120px'}) }}
{% endblock %}

{% block usage %}
{{ toolkit_code_usage(kit_id.value, component.name) }}
{% endblock %}

{% block accessibility %}
Always provide a concise `label` that identifies what the button closes or dismisses. The visible icon is decorative and the accessible name comes from `aria-label`.

Use the disabled state only when the close action is genuinely unavailable.
{% endblock %}

{% block examples %}
### Example
Render a standard close button with an accessible label.
{{ toolkit_code_example(kit_id.value, component.name, 'Example', {height: '120px'}) }}

### Disabled state
Disable the button when the dismiss action is unavailable.
{{ toolkit_code_example(kit_id.value, component.name, 'Disabled state', {height: '120px'}) }}

### Dark variant
Use Bootstrap's `data-bs-theme="dark"` color mode on the button or an ancestor. The deprecated `.btn-close-white` class is intentionally not used.
{{ toolkit_code_example(kit_id.value, component.name, 'Dark variant', {height: '120px'}) }}
{% endblock %}
