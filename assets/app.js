import './styles/app.css';

// <iconify-icon> renders the icon browser's SVGs from Iconify's batched JSON
// API, which is not rate limited the way their SVG endpoint is
import 'iconify-icon';

// start the Stimulus application
import './bootstrap.js';
