{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name, {height: '210px'}) }}
{% endblock %}

{% block usage %}
{{ toolkit_code_usage(kit_id.value, component.name) }}
{% endblock %}

{% block accessibility %}
Do not rely on contextual color alone to communicate meaning. Make the meaning explicit in the visible content or provide additional visually hidden text.

Disabled links omit their `href`, expose `aria-disabled="true"`, and are removed from keyboard navigation. Associate every checkbox and radio with a visible label or an `aria-label`.
{% endblock %}

{% block examples %}
### Basic example
Use a list group to display a simple series of related items.
{{ toolkit_code_example(kit_id.value, component.name, 'Basic example', {height: '250px'}) }}

### Active items
Mark the current selection with the active state.
{{ toolkit_code_example(kit_id.value, component.name, 'Active items', {height: '250px'}) }}

### Links and buttons
Render actionable list items as links or buttons to expose hover, active, and disabled states.
{{ toolkit_code_example(kit_id.value, component.name, 'Links and buttons', {height: '470px'}) }}

### Flush
Remove outer borders and rounded corners for edge-to-edge rendering inside a parent container.
{{ toolkit_code_example(kit_id.value, component.name, 'Flush', {height: '250px'}) }}

### Numbered
Generate item numbers with Bootstrap's CSS counters, including for items with custom content.
{{ toolkit_code_example(kit_id.value, component.name, 'Numbered', {height: '380px'}) }}

### Horizontal
Display items horizontally at every breakpoint or from a selected responsive breakpoint.
{{ toolkit_code_example(kit_id.value, component.name, 'Horizontal', {height: '660px'}) }}

### Variants
Apply contextual colors to non-actionable list items.
{{ toolkit_code_example(kit_id.value, component.name, 'Variants', {height: '420px'}) }}

### Action variants
Combine contextual colors with actionable links and their hover and active states.
{{ toolkit_code_example(kit_id.value, component.name, 'Action variants', {height: '420px'}) }}

### With badges
Add badges to communicate counts or activity alongside an item label.
{{ toolkit_code_example(kit_id.value, component.name, 'With badges', {height: '170px'}) }}

### Custom content
Compose headings, supporting text, metadata, and other HTML within actionable items.
{{ toolkit_code_example(kit_id.value, component.name, 'Custom content', {height: '330px'}) }}

### Checkboxes and radios
Place labeled form controls inside list items, with an optional stretched label for a larger click target.
{{ toolkit_code_example(kit_id.value, component.name, 'Checkboxes and radios', {height: '450px'}) }}

### JavaScript behavior
Use Bootstrap's tab plugin and data attributes to connect list items with tab panels.
{{ toolkit_code_example(kit_id.value, component.name, 'JavaScript behavior', {height: '210px'}) }}
{% endblock %}
