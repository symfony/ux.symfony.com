{# Override of @UXToolkit/markdown/alert.html.twig: reuse the site's Alert rendering.
   The Toolkit passes the same variables: `variant` and `content`. #}
{{ include('common_mark/alert.html.twig') }}
