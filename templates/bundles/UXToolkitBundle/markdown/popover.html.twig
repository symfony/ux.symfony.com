{# Override of @UXToolkit/markdown/popover.html.twig: reuse the site's Popover rendering.
   The Toolkit passes the same variables: `id` and `description`. #}
{{ include('common_mark/popover.html.twig') }}
