{#-
    Presentational form textarea. The value can be passed via `value` (form
    theme) or as slot content (direct usage).
-#}
{%- props name = null, id = null, value = null, hasError = false -%}
<textarea
    {%- if name is not null %} name="{{ name }}"{% endif %}
    {%- if id is not null %} id="{{ id }}"{% endif %}
    class="{{ ('w-full bg-body border rounded-md px-3 py-1.5' ~ (hasError ? ' border-red-500 dark:border-red-400') ~ ' ' ~ attributes.render('class'))|tailwind_merge }}"
    {{- attributes.without('class') }}
>{{ value }}{% block content %}{% endblock %}</textarea>
