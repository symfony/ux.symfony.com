{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name, {height: '120px'}) }}
{% endblock %}

{% block usage %}
{{ toolkit_code_usage(kit_id.value, component.name) }}
{% endblock %}

{% block accessibility %}
Do not rely on button color alone to communicate meaning. Use explicit labels or additional visually hidden text when needed.

Disabled links omit their `href`, expose `aria-disabled="true"`, and are removed from keyboard navigation.
{% endblock %}

{% block examples %}
### Base class
Use the base Bootstrap button class without a contextual color when defining a custom style.
{{ toolkit_code_example(kit_id.value, component.name, 'Base class', {height: '120px'}) }}

### Variants
Use contextual colors to communicate the purpose of an action.
{{ toolkit_code_example(kit_id.value, component.name, 'Variants', {height: '120px'}) }}

### Disable text wrapping
Add Bootstrap's `text-nowrap` utility when a button label must remain on one line.
{{ toolkit_code_example(kit_id.value, component.name, 'Disable text wrapping', {height: '120px'}) }}

### Button tags
Render the component as a button, link, or input depending on the semantic element required.
{{ toolkit_code_example(kit_id.value, component.name, 'Button tags', {height: '120px'}) }}

### Outline buttons
Use outline styles for actions that need less visual weight.
{{ toolkit_code_example(kit_id.value, component.name, 'Outline buttons', {height: '120px'}) }}

### Sizes
Use Bootstrap's large and small sizes, or customize the component with Bootstrap CSS variables.
{{ toolkit_code_example(kit_id.value, component.name, 'Sizes', {height: '200px'}) }}

### Disabled state
Disable buttons and links while preserving the appropriate HTML and accessibility semantics.
{{ toolkit_code_example(kit_id.value, component.name, 'Disabled state', {height: '160px'}) }}

### Block buttons
Combine the component with Bootstrap layout utilities to create responsive full-width buttons.
{{ toolkit_code_example(kit_id.value, component.name, 'Block buttons', {height: '470px'}) }}

### Toggle states
Use Bootstrap's button plugin for controls that toggle between pressed and unpressed states.
{{ toolkit_code_example(kit_id.value, component.name, 'Toggle states', {height: '280px'}) }}
{% endblock %}
