{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name, {height: '220px'}) }}
{% endblock %}

{% block usage %}
{{ toolkit_code_usage(kit_id.value, component.name) }}
{% endblock %}

{% block accessibility %}
Bootstrap dropdowns are generic popovers, so the component does not add ARIA menu roles automatically. Add `role="menu"`, `role="menuitem"`, and the matching keyboard behavior only when the dropdown implements the complete ARIA menu pattern.

Use a button for actions and reserve link toggles and items for navigation. Give a toggle an `id` and pass it to the menu's `labelledBy` prop when an explicit accessible relationship is useful. Disabled links receive `aria-disabled="true"` and are removed from sequential keyboard navigation, but application code must still prevent any custom activation behavior.
{% endblock %}

{% block examples %}
### Single button
Use buttons or links with Bootstrap's contextual colors.
{{ toolkit_code_example(kit_id.value, component.name, 'Single button', {height: '300px'}) }}

### Split button
Separate the primary action from the menu toggle.
{{ toolkit_code_example(kit_id.value, component.name, 'Split button', {height: '300px'}) }}

### Sizing
Create large and small regular or split dropdown buttons.
{{ toolkit_code_example(kit_id.value, component.name, 'Sizing', {height: '420px'}) }}

### Dark dropdowns
Render a dark menu with a visible active item.
{{ toolkit_code_example(kit_id.value, component.name, 'Dark dropdowns', {height: '260px'}) }}

### Directions
Open menus from the center, above, or from either side.
{{ toolkit_code_example(kit_id.value, component.name, 'Directions', {height: '360px'}) }}

### Menu items
Use links, buttons, or non-interactive text as menu content.
{{ toolkit_code_example(kit_id.value, component.name, 'Menu items', {height: '250px'}) }}

### Active and disabled items
Communicate the current item and unavailable choices.
{{ toolkit_code_example(kit_id.value, component.name, 'Active and disabled items', {height: '220px'}) }}

### Menu alignment
Align a menu against the end of its toggle.
{{ toolkit_code_example(kit_id.value, component.name, 'Menu alignment', {height: '220px'}) }}

### Responsive alignment
Change menu alignment at Bootstrap breakpoints.
{{ toolkit_code_example(kit_id.value, component.name, 'Responsive alignment', {height: '240px'}) }}

### Alignment options
Combine responsive alignment with dropdowns that open in different directions.
{{ toolkit_code_example(kit_id.value, component.name, 'Alignment options', {height: '520px'}) }}

### Headers, dividers, and text
Structure longer menus with headings, separators, and explanatory copy.
{{ toolkit_code_example(kit_id.value, component.name, 'Headers dividers and text', {height: '300px'}) }}

### Forms
Display a static form or open one from a dropdown toggle.
{{ toolkit_code_example(kit_id.value, component.name, 'Forms', {height: '400px'}) }}

### Dropdown options
Configure Popper's offset and reference element with Bootstrap data attributes.
{{ toolkit_code_example(kit_id.value, component.name, 'Dropdown options', {height: '230px'}) }}

### Auto close behavior
Choose which inside or outside interactions dismiss the menu.
{{ toolkit_code_example(kit_id.value, component.name, 'Auto close behavior', {height: '280px'}) }}
{% endblock %}
