{# Override of @UXToolkit/markdown/tabs.html.twig: reuse ux.symfony.com's Shadcn Tabs component.
   The Toolkit passes the same variables this template needs: `tabs` (list of {id, title, content})
   and `active_tab_id`. #}
{{ include('common_mark/tabs.html.twig') }}
