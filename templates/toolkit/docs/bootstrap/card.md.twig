{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name, {height: '480px'}) }}
{% endblock %}

{% block usage %}
{{ toolkit_code_usage(kit_id.value, component.name) }}
{% endblock %}

{% block accessibility %}
Choose a semantic heading level for each card title that fits the surrounding page hierarchy.

Do not rely on background, text, or border color alone to communicate meaning. Keep sufficient contrast and provide an equivalent text cue when color carries information.
{% endblock %}

{% block examples %}
### Example
Combine an image, title, supporting text, and action in a fixed-width card.
{{ toolkit_code_example(kit_id.value, component.name, 'Example', {height: '400px'}) }}

### Content types
Mix card bodies, titles, subtitles, text, links, images, and list groups.
{{ toolkit_code_example(kit_id.value, component.name, 'Content types', {height: '470px'}) }}

### Header and footer
Add optional headers and footers, including semantic heading and quote content.
{{ toolkit_code_example(kit_id.value, component.name, 'Header and footer', {height: '420px'}) }}

### Sizing
Size cards with Bootstrap's grid, width utilities, or custom CSS.
{{ toolkit_code_example(kit_id.value, component.name, 'Sizing', {height: '190px'}) }}

### Text alignment
Apply Bootstrap text alignment utilities to a card or one of its sections.
{{ toolkit_code_example(kit_id.value, component.name, 'Text alignment', {height: '440px'}) }}

### Navigation
Place tabs or pills in the card header.
{{ toolkit_code_example(kit_id.value, component.name, 'Navigation', {height: '350px'}) }}

### Image caps
Position an image at the top or bottom edge of a card.
{{ toolkit_code_example(kit_id.value, component.name, 'Image caps', {height: '350px'}) }}

### Image overlays
Use an image as the card background and place concise, high-contrast content over it.
{{ toolkit_code_example(kit_id.value, component.name, 'Image overlays', {height: '410px'}) }}

### Horizontal
Combine the component with Bootstrap's responsive grid to create a horizontal card.
{{ toolkit_code_example(kit_id.value, component.name, 'Horizontal', {height: '210px'}) }}

### Background and color
Use Bootstrap's `text-bg-*` helpers for contextual card styles.
{{ toolkit_code_example(kit_id.value, component.name, 'Background and color', {height: '720px'}) }}

### Borders
Change border and text colors independently with utility classes.
{{ toolkit_code_example(kit_id.value, component.name, 'Borders', {height: '720px'}) }}

### Mixins utilities
Customize header and footer borders or remove their background color.
{{ toolkit_code_example(kit_id.value, component.name, 'Mixins utilities', {height: '250px'}) }}

### Card groups
Render a set of attached cards with equal width and height.
{{ toolkit_code_example(kit_id.value, component.name, 'Card groups', {height: '370px'}) }}

### Grid cards
Use responsive grid columns and `h-100` cards for a flexible multi-card layout.
{{ toolkit_code_example(kit_id.value, component.name, 'Grid cards', {height: '360px'}) }}
{% endblock %}
